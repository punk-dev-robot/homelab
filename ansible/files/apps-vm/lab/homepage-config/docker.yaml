---
# https://gethomepage.dev/configs/docker/
# apps-vm only: via the oam stack's socket-proxy (see lab/homepage.yml — direct socket mount is EACCES for homepage's non-root server).
# Remote VMs (media/obs) would need docker TCP exposure — pass 2.
apps-docker:
  host: socket-proxy
  port: 2375
