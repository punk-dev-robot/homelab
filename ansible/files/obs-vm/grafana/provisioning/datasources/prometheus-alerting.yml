apiVersion: 1

# Provisioned alongside the UI-created datasource (which has an unknown uid);
# alert rules need a stable uid, so they get their own datasource entry.
datasources:
  - name: Prometheus (alerting)
    uid: prom-alerts
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false
